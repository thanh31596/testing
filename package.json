{
  "name": "react",
  "version": "1.1.0",
  "description": "an example for deploying a React + NodeJS app to Heroku",
  "main": "server.js",
  "scripts": {
    "build-client": "server --config server/index.js",
    "build":"npm run build-client",
    "review": "npm test",
    "start": "npm run build && NODE_ENV=production node server/index.js"
  }
}
 
