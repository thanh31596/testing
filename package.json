{
  "name": "react",
  "version": "1.1.0",
  "description": "an example for deploying a React + NodeJS app to Heroku",
  "main": "server.js",
  "scripts": {
    "clean": "rimraf build static/dist",
    "build-client": "webpack --config server/index.js",
    "build": "npm run clean && npm run build-server && npm run build-client",
    "review": "npm test",
    "start": "npm run build && NODE_ENV=production node server/index.js"
  }
}
 
